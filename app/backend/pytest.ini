[pytest]
asyncio_mode = auto
markers =
    integration: hits the real running dev server (localhost:8001), a real Postgres DB, and/or real external network (Ollama, OASIS schema servers). Skips gracefully if unreachable rather than failing the whole suite.
